name: dotnet package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: ['6.0.x']

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: ${{ matrix.dotnet-version }}

      - name: Install dependencies
        run: dotnet restore FileSplitter/FileSplitter.csproj

      - name: Build
        run: dotnet build FileSplitter/FileSplitter.csproj --configuration Release --no-restore
        
      - name: Install Test dependencies
        run: dotnet restore FileSplitter.Tests/FileSplitter.Tests.csproj        

      - name: Test
        run: dotnet test FileSplitter.Tests/FileSplitter.Tests.csproj --no-restore --verbosity normal